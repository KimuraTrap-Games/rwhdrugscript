-- Placeholder AI logic
function TryAIEncounter(coords)
    local roll = math.random(1, 100)
    if roll <= Config.EncounterChance then
        -- spawn hostile or passive NPC (future)
    end
end
